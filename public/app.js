// State Management
let state = {
  products: [],
  addons: [],
  cart: [],
  currentPage: 'home',
  currentCategory: 'all',
  deliveryData: {
    type: 'pickup',
    cost: 0,
    duration: 'فوري'
  }
};

// Initialize app
document.addEventListener('DOMContentLoaded', () => {
  loadProducts();
  loadAddons();
  navigateTo('home');
});

// Navigation
function navigateTo(page) {
  // Hide all pages
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  
  // Show selected page
  const targetPage = document.getElementById(page);
  if (targetPage) {
    targetPage.classList.add('active');
    state.currentPage = page;
    
    // Load page specific content
    if (page === 'menu') {
      displayProducts();
    } else if (page === 'cart') {
      displayCart();
    }
  }
}

// Load products from server
async function loadProducts() {
  try {
    const response = await fetch('/api/products');
    state.products = await response.json();
    displayProducts();
  } catch (error) {
    console.error('Error loading products:', error);
    // Load sample products if server not available
    loadSampleProducts();
  }
}

// Load sample products (for demo)
function loadSampleProducts() {
  state.products = [
    // Strawberry Category (6 items)
    { id: 1, name: 'فراولة فريش', category: 'strawberry', price: 25, image: 'https://images.unsplash.com/photo-1563805042-7684c019e1cb?w=300&h=300&fit=crop', description: 'مثلجات فراولة طازة وناعمة' },
    { id: 2, name: 'فراولة مع كريم', category: 'strawberry', price: 30, image: 'https://images.unsplash.com/photo-1599599810694-b5ac4dd37e49?w=300&h=300&fit=crop', description: 'فراولة مع كريم سميك فاخر' },
    { id: 3, name: 'فراولة ديلوكس', category: 'strawberry', price: 35, image: 'https://images.unsplash.com/photo-1563805042-7684c019e1cb?w=300&h=300&fit=crop', description: 'مثلجات فراولة بريميوم' },
    { id: 4, name: 'فراولة باتشينو', category: 'strawberry', price: 28, image: 'https://images.unsplash.com/photo-1599599810694-b5ac4dd37e49?w=300&h=300&fit=crop', description: 'فراولة مع قطع الفاكهة' },
    { id: 5, name: 'فراولة ميني', category: 'strawberry', price: 15, image: 'https://images.unsplash.com/photo-1563805042-7684c019e1cb?w=300&h=300&fit=crop', description: 'حجم صغير من الفراولة' },
    { id: 6, name: 'فراولة كونج', category: 'strawberry', price: 40, image: 'https://images.unsplash.com/photo-1599599810694-b5ac4dd37e49?w=300&h=300&fit=crop', description: 'فراولة مع اضافات متعددة' },\n\n    // Chocolate Category (6 items)\n    { id: 7, name: 'شوكولاتة سوداء', category: 'chocolate', price: 25, image: 'https://images.unsplash.com/photo-1578985545062-69928b1d9587?w=300&h=300&fit=crop', description: 'شوكولاتة سوداء غنية' },\n    { id: 8, name: 'شوكولاتة بيضاء', category: 'chocolate', price: 28, image: 'https://images.unsplash.com/photo-1578985545062-69928b1d9587?w=300&h=300&fit=crop', description: 'شوكولاتة بيضاء ناعمة' },\n    { id: 9, name: 'شوكولاتة فادج', category: 'chocolate', price: 32, image: 'https://images.unsplash.com/photo-1578985545062-69928b1d9587?w=300&h=300&fit=crop', description: 'شوكولاتة مع فادج غني' },\n    { id: 10, name: 'شوكولاتة برالين', category: 'chocolate', price: 35, image: 'https://images.unsplash.com/photo-1578985545062-69928b1d9587?w=300&h=300&fit=crop', description: 'شوكولاتة مع برالين' },\n    { id: 11, name: 'شوكولاتة نوتيلا', category: 'chocolate', price: 30, image: 'https://images.unsplash.com/photo-1578985545062-69928b1d9587?w=300&h=300&fit=crop', description: 'شوكولاتة مع نوتيلا' },\n    { id: 12, name: 'شوكولاتة دبل', category: 'chocolate', price: 38, image: 'https://images.unsplash.com/photo-1578985545062-69928b1d9587?w=300&h=300&fit=crop', description: 'شوكولاتة مضاعفة' },\n\n    // Vanilla Category (3 items)\n    { id: 13, name: 'فانيليا بيور', category: 'vanilla', price: 22, image: 'https://images.unsplash.com/photo-1563805042-7684c019e1cb?w=300&h=300&fit=crop', description: 'فانيليا نقية وناعمة' },\n    { id: 14, name: 'فانيليا ديلوكس', category: 'vanilla', price: 28, image: 'https://images.unsplash.com/photo-1563805042-7684c019e1cb?w=300&h=300&fit=crop', description: 'فانيليا فاخرة' },\n    { id: 15, name: 'فانيليا تيرابن', category: 'vanilla', price: 25, image: 'https://images.unsplash.com/photo-1563805042-7684c019e1cb?w=300&h=300&fit=crop', description: 'فانيليا مع ترابل' },\n\n    // Pistachio Category (2 items)\n    { id: 16, name: 'فستق إيطالي', category: 'pistachio', price: 30, image: 'https://images.unsplash.com/photo-1599599810694-b5ac4dd37e49?w=300&h=300&fit=crop', description: 'فستق إيطالي أصلي' },\n    { id: 17, name: 'فستق بريميوم', category: 'pistachio', price: 35, image: 'https://images.unsplash.com/photo-1599599810694-b5ac4dd37e49?w=300&h=300&fit=crop', description: 'فستق من أفضل الأنواع' },\n\n    // Special Category (2 items)\n    { id: 18, name: 'ميكس بارفيه', category: 'special', price: 45, image: 'https://images.unsplash.com/photo-1563805042-7684c019e1cb?w=300&h=300&fit=crop', description: 'خليط مميز من 3 نكهات' },\n    { id: 19, name: 'كوب جومبو', category: 'special', price: 50, image: 'https://images.unsplash.com/photo-1599599810694-b5ac4dd37e49?w=300&h=300&fit=crop', description: 'كوب عملاق فاخر' }\n  ];\n}

// Load addons
async function loadAddons() {
  try {\n    const response = await fetch('/api/addons');\n    state.addons = await response.json();\n  } catch (error) {\n    console.error('Error loading addons:', error);\n    loadSampleAddons();\n  }\n}\n\n// Load sample addons\nfunction loadSampleAddons() {\n  state.addons = [\n    { id: 1, name: 'صلصة الشوكولاتة', price: 5 },\n    { id: 2, name: 'صلصة الكراميل', price: 5 },\n    { id: 3, name: 'المكسرات المشكلة', price: 8 },\n    { id: 4, name: 'قطع الشوكولاتة', price: 6 },\n    { id: 5, name: 'جوز الهند المبشور', price: 5 },\n    { id: 6, name: 'حبات الفانيليا', price: 4 },\n    { id: 7, name: 'شراب الفراولة', price: 5 },\n    { id: 8, name: 'كريم خفيف', price: 3 }\n  ];\n}\n\n// Display products\nfunction displayProducts() {\n  const grid = document.getElementById('productsGrid');\n  if (!grid) return;\n  \n  let filteredProducts = state.products;\n  if (state.currentCategory !== 'all') {\n    filteredProducts = state.products.filter(p => p.category === state.currentCategory);\n  }\n  \n  grid.innerHTML = filteredProducts.map(product => `\n    <div class=\"product-card\">\n      <img src=\"${product.image}\" alt=\"${product.name}\" class=\"product-image\">\n      <div class=\"product-info\">\n        <div class=\"product-name\">${product.name}</div>\n        <div class=\"product-description\">${product.description}</div>\n        <div class=\"product-price\">${product.price}$</div>\n        \n        <div class=\"product-addons\" id=\"addons-${product.id}\">\n          ${state.addons.map(addon => `\n            <label class=\"addon-check\">\n              <input type=\"checkbox\" value=\"${addon.id}\" class=\"addon-checkbox\" data-addon-price=\"${addon.price}\">\n              <span>${addon.name} (+${addon.price}$)</span>\n            </label>\n          `).join('')}\n        </div>\n        \n        <div class=\"product-actions\">\n          <div class=\"quantity-control\">\n            <button class=\"qty-btn\" onclick=\"changeQty(${product.id}, -1)\">−</button>\n            <input type=\"text\" class=\"qty-input\" id=\"qty-${product.id}\" value=\"1\" readonly>\n            <button class=\"qty-btn\" onclick=\"changeQty(${product.id}, 1)\">+</button>\n          </div>\n          <button class=\"add-to-cart-btn\" onclick=\"addToCart(${product.id})\">أضف</button>\n        </div>\n      </div>\n    </div>\n  `).join('');\n}\n\n// Filter products by category\nfunction filterCategory(category) {\n  state.currentCategory = category;\n  document.querySelectorAll('.filter-btn').forEach(btn => btn.classList.remove('active'));\n  event.target.classList.add('active');\n  displayProducts();\n}\n\n// Change quantity\nfunction changeQty(productId, change) {\n  const input = document.getElementById(`qty-${productId}`);\n  let value = parseInt(input.value) + change;\n  if (value < 1) value = 1;\n  input.value = value;\n}\n\n// Add to cart\nfunction addToCart(productId) {\n  const product = state.products.find(p => p.id === productId);\n  if (!product) return;\n  \n  const qtyInput = document.getElementById(`qty-${productId}`);\n  const quantity = parseInt(qtyInput.value);\n  \n  // Get selected addons\n  const addonsContainer = document.getElementById(`addons-${productId}`);\n  const selectedAddons = [];\n  let addonCost = 0;\n  \n  if (addonsContainer) {\n    addonsContainer.querySelectorAll('.addon-checkbox:checked').forEach(checkbox => {\n      const addonId = parseInt(checkbox.value);\n      const addonPrice = parseFloat(checkbox.dataset.addonPrice);\n      const addon = state.addons.find(a => a.id === addonId);\n      if (addon) {\n        selectedAddons.push({ ...addon, price: addonPrice });\n        addonCost += addonPrice;\n      }\n    });\n  }\n  \n  // Check if product already in cart\n  const existingItem = state.cart.find(item => \n    item.id === productId && \n    JSON.stringify(item.addons) === JSON.stringify(selectedAddons)\n  );\n  \n  if (existingItem) {\n    existingItem.quantity += quantity;\n  } else {\n    state.cart.push({\n      id: productId,\n      name: product.name,\n      price: product.price,\n      image: product.image,\n      quantity: quantity,\n      addons: selectedAddons,\n      addonCost: addonCost\n    });\n  }\n  \n  updateCartCount();\n  // Reset quantity\n  qtyInput.value = '1';\n  \n  // Show confirmation\n  showNotification(`تم إضافة ${product.name} إلى السلة`);\n}\n\n// Show notification\nfunction showNotification(message) {\n  const notification = document.createElement('div');\n  notification.style.cssText = `\n    position: fixed;\n    top: 20px;\n    right: 20px;\n    background: var(--primary-color);\n    color: white;\n    padding: 15px 25px;\n    border-radius: 50px;\n    box-shadow: 0 5px 20px rgba(255, 107, 53, 0.4);\n    z-index: 3000;\n    animation: slideDown 0.3s ease;\n  `;\n  notification.textContent = message;\n  document.body.appendChild(notification);\n  \n  setTimeout(() => {\n    notification.style.animation = 'fadeOut 0.3s ease';\n    setTimeout(() => notification.remove(), 300);\n  }, 3000);\n}\n\n// Update cart count\nfunction updateCartCount() {\n  const cartCount = document.querySelector('.cart-count');\n  if (cartCount) {\n    cartCount.textContent = state.cart.reduce((sum, item) => sum + item.quantity, 0);\n  }\n}\n\n// Display cart\nfunction displayCart() {\n  const cartItemsContainer = document.getElementById('cartItems');\n  if (!cartItemsContainer) return;\n  \n  if (state.cart.length === 0) {\n    cartItemsContainer.innerHTML = `\n      <div class=\"empty-cart\">\n        <i class=\"fas fa-shopping-cart\"></i>\n        <p>السلة فارغة</p>\n        <button class=\"btn btn-primary\" onclick=\"navigateTo('menu')\">تصفح المنتجات</button>\n      </div>\n    `;\n    updateCartSummary();\n    return;\n  }\n  \n  cartItemsContainer.innerHTML = state.cart.map((item, index) => `\n    <div class=\"cart-item\">\n      <img src=\"${item.image}\" alt=\"${item.name}\" class=\"cart-item-image\">\n      <div class=\"cart-item-content\">\n        <div class=\"cart-item-name\">${item.name}</div>\n        <div class=\"cart-item-price\">${item.price}$ × ${item.quantity} = ${(item.price * item.quantity).toFixed(2)}$</div>\n        ${item.addons.length > 0 ? `\n          <div class=\"cart-item-addons\">\n            <strong>الإضافات:</strong> ${item.addons.map(a => `${a.name} (${a.price}$)`).join(', ')}\n          </div>\n        ` : ''}\n        <div class=\"cart-item-qty\">\n          <button class=\"qty-btn\" onclick=\"updateCartQty(${index}, -1)\">−</button>\n          <input type=\"text\" class=\"qty-input\" value=\"${item.quantity}\" readonly>\n          <button class=\"qty-btn\" onclick=\"updateCartQty(${index}, 1)\">+</button>\n          <button class=\"cart-item-remove\" onclick=\"removeFromCart(${index})\">\n            <i class=\"fas fa-trash\"></i>\n          </button>\n        </div>\n      </div>\n    </div>\n  `).join('');\n  \n  updateCartSummary();\n}\n\n// Update cart item quantity\nfunction updateCartQty(index, change) {\n  state.cart[index].quantity += change;\n  if (state.cart[index].quantity < 1) {\n    removeFromCart(index);\n  } else {\n    displayCart();\n    updateCartCount();\n  }\n}\n\n// Remove from cart\nfunction removeFromCart(index) {\n  state.cart.splice(index, 1);\n  displayCart();\n  updateCartCount();\n}\n\n// Update cart summary\nfunction updateCartSummary() {\n  // Calculate subtotal\n  let subtotal = 0;\n  state.cart.forEach(item => {\n    subtotal += (item.price * item.quantity) + (item.addonCost * item.quantity);\n  });\n  \n  // Apply delivery cost\n  const deliveryCost = state.deliveryData.type === 'delivery' ? 25 : 0;\n  \n  // Calculate discount (if applicable)\n  let discount = 0;\n  if (subtotal > 100) {\n    discount = subtotal * 0.1; // 10% discount for orders over 100$\n  }\n  \n  // Calculate total\n  const total = subtotal - discount + deliveryCost;\n  \n  // Update display\n  document.getElementById('subtotal').textContent = subtotal.toFixed(2) + '$';\n  document.getElementById('deliveryCost').textContent = deliveryCost.toFixed(2) + '$';\n  \n  const discountItem = document.getElementById('discountItem');\n  if (discount > 0) {\n    discountItem.style.display = 'flex';\n    document.getElementById('discountAmount').textContent = discount.toFixed(2) + '$';\n  } else {\n    discountItem.style.display = 'none';\n  }\n  \n  document.getElementById('totalPrice').textContent = total.toFixed(2) + '$';\n}\n\n// Update delivery option\nfunction updateDelivery() {\n  const selected = document.querySelector('input[name=\"delivery\"]:checked').value;\n  state.deliveryData.type = selected;\n  \n  if (selected === 'delivery') {\n    state.deliveryData.cost = 25;\n    state.deliveryData.duration = '15-20 دقيقة';\n  } else {\n    state.deliveryData.cost = 0;\n    state.deliveryData.duration = 'فوري';\n  }\n  \n  updateCartSummary();\n}\n\n// Send order via WhatsApp\nfunction sendViaWhatsapp() {\n  if (state.cart.length === 0) {\n    showNotification('السلة فارغة!');\n    return;\n  }\n  \n  // Generate order number\n  const orderNumber = 'ORD-' + Date.now();\n  \n  // Get notes\n  const notes = document.getElementById('orderNotes').value;\n  \n  // Calculate totals\n  let subtotal = 0;\n  state.cart.forEach(item => {\n    subtotal += (item.price * item.quantity) + (item.addonCost * item.quantity);\n  });\n  \n  const deliveryCost = state.deliveryData.type === 'delivery' ? 25 : 0;\n  let discount = 0;\n  if (subtotal > 100) {\n    discount = subtotal * 0.1;\n  }\n  const total = subtotal - discount + deliveryCost;\n  \n  // Build WhatsApp message\n  let message = `*طلب جديد من متجر مثلجات هاواي* 🍦\\n\\n`;\n  message += `*رقم الطلب:* ${orderNumber}\\n\\n`;\n  message += `*المنتجات:*\\n`;\n  \n  state.cart.forEach(item => {\n    const itemTotal = (item.price + item.addonCost) * item.quantity;\n    message += `• ${item.name} (${item.quantity}) - ${itemTotal.toFixed(2)}$\\n`;\n    \n    if (item.addons.length > 0) {\n      item.addons.forEach(addon => {\n        message += `  ├ ${addon.name} (+${addon.price}$)\\n`;\n      });\n    }\n  });\n  \n  message += `\\n*طريقة الاستلام:* ${state.deliveryData.type === 'delivery' ? 'توصيل' : 'استلام من المتجر'}\\n`;\n  \n  if (state.deliveryData.type === 'delivery') {\n    message += `*تكلفة التوصيل:* ${deliveryCost}$\\n`;\n    message += `*مدة التوصيل:* ${state.deliveryData.duration}\\n`;\n  }\n  \n  if (notes) {\n    message += `\\n*ملاحظات:* ${notes}\\n`;\n  }\n  \n  message += `\\n*الإجمالي:* ${total.toFixed(2)}$`;\n  \n  // Send to WhatsApp\n  const phone = '963956492267';\n  const waLink = `https://wa.me/${phone}?text=${encodeURIComponent(message)}`;\n  \n  // Open WhatsApp\n  window.open(waLink, '_blank');\n  \n  // Show modal after a delay\n  setTimeout(() => {\n    showWhatsappModal();\n  }, 500);\n}\n\n// Show WhatsApp confirmation modal\nfunction showWhatsappModal() {\n  const modal = document.getElementById('whatsappModal');\n  modal.classList.add('show');\n}\n\n// Close modal\nfunction closeModal() {\n  const modal = document.getElementById('whatsappModal');\n  modal.classList.remove('show');\n}\n\n// Clear cart\nfunction clearCart() {\n  state.cart = [];\n  updateCartCount();\n  closeModal();\n  navigateTo('home');\n  showNotification('تم إفراغ السلة بنجاح');\n}\n\n// Add product (Admin panel)\nfunction addProduct(event) {\n  event.preventDefault();\n  \n  const name = document.getElementById('productName').value;\n  const category = document.getElementById('productCategory').value;\n  const price = parseFloat(document.getElementById('productPrice').value);\n  const image = document.getElementById('productImage').value;\n  const description = document.getElementById('productDescription').value;\n  \n  const newProduct = {\n    id: Date.now(),\n    name,\n    category,\n    price,\n    image,\n    description\n  };\n  \n  // Add to local state\n  state.products.push(newProduct);\n  \n  // Try to save to server\n  fetch('/api/products', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify(newProduct)\n  }).catch(err => console.log('Server not available, saved locally'));\n  \n  document.getElementById('productForm').reset();\n  showNotification(`تم إضافة المنتج \"${name}\" بنجاح`);\n}\n\n// Add addon (Admin panel)\nfunction addAddon(event) {\n  event.preventDefault();\n  \n  const name = document.getElementById('addonName').value;\n  const price = parseFloat(document.getElementById('addonPrice').value);\n  \n  const newAddon = {\n    id: Date.now(),\n    name,\n    price\n  };\n  \n  // Add to local state\n  state.addons.push(newAddon);\n  \n  // Try to save to server\n  fetch('/api/addons', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify(newAddon)\n  }).catch(err => console.log('Server not available, saved locally'));\n  \n  document.getElementById('addonForm').reset();\n  showNotification(`تم إضافة الإضافة \"${name}\" بنجاح`);\n  \n  // Reload products to show new addon\n  displayProducts();\n}\n